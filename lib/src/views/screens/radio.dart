import 'package:flutter/material.dart';

class RadioStation extends StatelessWidget {
  const RadioStation({key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
